server:
  port: 8083

spring:
  application:
    name: pricing-service
  data:
    mongodb:
      uri: mongodb://mongo:27017/pricing
      auto-index-creation: true

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
